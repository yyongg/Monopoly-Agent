# Runtime dependencies for playing the game.
# Install with:  pip install -r requirements.txt
#
# The game engine itself is pure standard library; the single dependency below
# is needed only for the desktop pygame UI (play_gui.py).

pygame==2.6.1
